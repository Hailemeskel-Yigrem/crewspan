services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: relayops
      POSTGRES_PASSWORD: relayops
      POSTGRES_DB: relayops
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U relayops"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      RELAYOPS_DATABASE_URL: postgresql+asyncpg://relayops:relayops@postgres:5432/relayops
      RELAYOPS_REDIS_URL: redis://redis:6379/0
      RELAYOPS_SECRET_KEY: dev-secret-change-in-production
      RELAYOPS_CORS_ORIGINS: '["http://localhost:3000"]'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - api

  worker:
    build:
      context: ./apps/worker
      dockerfile: Dockerfile
    environment:
      RELAYOPS_DATABASE_URL: postgresql+asyncpg://relayops:relayops@postgres:5432/relayops
      RELAYOPS_CELERY_BROKER_URL: redis://redis:6379/1
      RELAYOPS_CELERY_RESULT_BACKEND: redis://redis:6379/2
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker-beat:
    build:
      context: ./apps/worker
      dockerfile: Dockerfile.beat
    environment:
      RELAYOPS_CELERY_BROKER_URL: redis://redis:6379/1
      RELAYOPS_CELERY_RESULT_BACKEND: redis://redis:6379/2
    depends_on:
      - redis
      - worker

volumes:
  postgres_data:
